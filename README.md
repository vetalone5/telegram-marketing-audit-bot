# Telegram Marketing Audit Bot

Telegram-бот для автоматического анализа маркетинговых лендингов с помощью GPT-4o и интеграцией с Google Sheets.

## ✨ Возможности

- 🔍 **Автоматический анализ сайтов** - загрузка и анализ контента лендингов
- 🤖 **Анализ через GPT-4o** - детальный маркетинговый аудит с помощью ИИ
- 📊 **Интеграция с Google Sheets** - автоматическое сохранение результатов
- 💬 **Telegram интерфейс** - удобное взаимодействие через бота
- 🔒 **Система лимитов** - контроль использования для пользователей
- 📝 **Детальное логирование** - полное отслеживание операций

## 🏗️ Архитектура

```
app/
├── core/           # Основные утилиты и настройки
├── features/       # Бизнес-логика приложения
│   └── audit/      # Модуль анализа сайтов
├── storage/        # Работа с базой данных
└── telegram/       # Telegram бот интерфейс
```

## 🚀 Быстрый старт

### Требования

- Python 3.11+
- Poetry для управления зависимостями
- Telegram Bot Token
- OpenAI API Key
- Google Service Account (для Sheets)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd telegram-audit-bot
```

2. Установите зависимости:
```bash
poetry install
```

3. Создайте файл `.env`:
```env
TELEGRAM_TOKEN=your_telegram_bot_token
OPENAI_API_KEY=your_openai_api_key
GOOGLE_SHEETS_ID=your_default_sheet_id
ADMIN_USER_IDS=123456789,987654321
DEFAULT_USER_LIMIT=10
```

4. Добавьте Google Service Account JSON в `secrets/service_account.json`

5. Запустите бота:
```bash
poetry run python -m app.telegram.bot
```

## 📋 Основные функции

### Анализ сайта
- Загрузка контента главной страницы
- Поиск и анализ страницы с ценами
- Извлечение ключевой маркетинговой информации
- Генерация рекомендаций по улучшению

### Результаты анализа включают:
- Оффер и CTA первого экрана
- УТП и описание продукта/услуг
- Анализ болей целевой аудитории
- Цены и тарифы
- Факторы доверия и гарантии
- Маркетинговые выводы и гипотезы роста

### Google Sheets интеграция
- Автоматическое создание таблиц
- Сохранение результатов анализа
- Возможность подключения собственных таблиц
- Перенос отложенных результатов

## 🛠️ Технологии

- **Python 3.11** - основной язык
- **python-telegram-bot** - Telegram API
- **OpenAI GPT-4o** - анализ контента
- **Google Sheets API** - сохранение результатов
- **SQLite** - локальная база данных
- **httpx** - HTTP клиент для загрузки сайтов
- **BeautifulSoup** - парсинг HTML
- **Poetry** - управление зависимостями

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `TELEGRAM_TOKEN` | Токен Telegram бота | ✅ |
| `OPENAI_API_KEY` | Ключ OpenAI API | ✅ |
| `GOOGLE_SHEETS_ID` | ID резервной таблицы | ✅ |
| `ADMIN_USER_IDS` | ID администраторов (через запятую) | ✅ |
| `DEFAULT_USER_LIMIT` | Лимит анализов для новых пользователей | ❌ (по умолчанию: 10) |

### Флаги управления

В `app/features/audit/adapters/llm.py` и `fetcher.py`:
- `USE_WEB_TOOLS = False` - использование web tools для загрузки (по умолчанию отключено)

## 📊 Логирование

Все операции логируются в:
- Консоль (в реальном времени)
- `logs/bot.log` (основные события)
- `logs/errors.log` (ошибки)

Уровни логирования:
- `INFO` - основные операции
- `DEBUG` - детальная диагностика
- `ERROR` - ошибки и исключения

## 🧪 Тестирование

```bash
# Запуск всех тестов
poetry run pytest

# Запуск конкретного теста
poetry run pytest tests/test_cleaner.py

# Запуск с покрытием
poetry run pytest --cov=app
```

## 🤝 Команды администратора

- `/set_limit <user_id> <limit>` - установить лимит для пользователя

## 📝 Лицензия

MIT License

## 🔒 Безопасность

- Все секретные данные исключены из Git через `.gitignore`
- Файловая блокировка предотвращает множественные запуски
- Система лимитов контролирует использование ресурсов
- Валидация входных данных на всех уровнях

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в `logs/`
2. Убедитесь в корректности переменных окружения
3. Проверьте доступность внешних API (OpenAI, Telegram)